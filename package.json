{
  "name": "@apalchys/freemarker",
  "version": "2.0.1",
  "description": "Freemarker integration for NodeJS.",
  "main": "dist/app.js",
  "files": [
    "dist",
    "fmpp"
  ],
  "scripts": {
    "test": "node example/test.js",
    "dev": "esbuild src/app.js --bundle --outfile=dist/app.js --platform=node --target=node10 --watch",
    "build": "esbuild src/app.js --bundle --outfile=dist/app.js --platform=node --target=node10",
    "release": "npm run build && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kaola-fed/freemarker.git"
  },
  "keywords": [
    "Freemarker"
  ],
  "author": "Cody Chan <int64ago@gmail.com> (https://int64ago.org)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kaola-fed/freemarker/issues"
  },
  "homepage": "https://github.com/kaola-fed/freemarker#readme",
  "devDependencies": {
    "esbuild": "^0.12.15"
  }
}
